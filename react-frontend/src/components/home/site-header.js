import { Heading, Box, Link } from '@chakra-ui/react';

export const SiteHeader = () => {
  return (
    <Box mb={'30px'} backgroundColor={'blue'}>
      <Heading size={'2xl'} href='/'>Constructifyer Projects</Heading>
    </Box>
  );
};
